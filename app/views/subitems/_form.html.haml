.form-inline
  = form_for [item, subitem], remote: true do |f|
    .form-group
      = f.text_field :name, class: 'form-control', placeholder: "Create subitem"
    .form-group
      = f.submit "Save", class: 'btn btn-primary'
